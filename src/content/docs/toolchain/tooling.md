---
title: Editor integrations
---

The source includes a VS Code extension with syntax configuration and snippets, a Zed extension using a Tree-sitter grammar, and a language-server crate. The integrated graphical editor is **Work in progress**: a `kalcite-editor` crate exists, but its complete user workflow is not yet documented as stable.
