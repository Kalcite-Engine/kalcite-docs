# Recommended future GitHub rules

Protect `main`, require review and passing CI, prevent force pushes and branch deletion, require conversation resolution, and reserve maintainer bypass for exceptional recovery. Consider signed commits only when contributors have a practical path to use them.
